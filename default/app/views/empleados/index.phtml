<style>
    .content-header {
        background-color: #228B22; /* Verde forestal */
        color: white;
        padding: 10px 15px;
        border-radius: 4px;
    }

    .card-header {
        background-color: #6cc112; /* Verde */
        color: white;
    }

    .btn-secondary {
        background-color: #DC143C; /* Rojo carmesí */
        border-color: #c11236;
        color: white;
    }

    .btn-secondary:hover {
        background-color: #c11236;
        border-color: #a60f2e;
    }

    .table th {
        background-color: #f8f9fa;
        color: #228B22;
    }

    .table-bordered thead th {
        border-bottom: 2px solid #228B22;
    }

    .text-right {
        text-align: right;
    }

    .card {
        margin-bottom: 20px;
        border: 1px solid rgb(34, 139, 34);
        border-radius: 0.25rem;
    }
</style>
<div class="container mt-4">
    <h1>Empleados</h1>

    <div class="d-flex justify-content-between mb-3">
        <a href="<?= URL_APP ?>/main" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Regresar
        </a>
        <a href="<?= URL_APP ?>/empleados/registrar" class="btn btn-primary">
            <i class="fas fa-plus"></i> Nuevo Empleado
        </a>
    </div>

    <?php View::content(); ?>

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead class="thead-dark">
            <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>Email</th>
                <th>Teléfono</th>
                <th>Acciones</th>
            </tr>
            </thead>
            <tbody>
            <?php if (!empty($empleados)): ?>
                <?php foreach ($empleados as $emp): ?>
                    <tr>
                        <td><?= $emp->id ?></td>
                        <td><?= htmlentities($emp->nombre) ?></td>
                        <td><?= htmlentities($emp->email) ?></td>
                        <td><?= htmlentities($emp->telefono) ?></td>
                        <td>
                            <div class="btn-group">
                                <a href="<?= URL_APP ?>/empleados/show/<?= $emp->id ?>" class="btn btn-sm btn-info" title="Ver">
                                    <i class="fas fa-eye"></i>
                                </a>
                                <a href="<?= URL_APP ?>/empleados/editar/<?= $emp->id ?>" class="btn btn-sm btn-warning" title="Editar">
                                    <i class="fas fa-edit"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                <?php endforeach; ?>
            <?php else: ?>
                <tr>
                    <td colspan="5" class="text-center text-muted py-4">
                        No hay empleados registrados
                    </td>
                </tr>
            <?php endif; ?>
            </tbody>
        </table>
    </div>
</div>
