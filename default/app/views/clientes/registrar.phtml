<style>
    .content-header {
        background-color: #228B22; /* Verde forestal */
        color: white;
        padding: 10px 15px;
        border-radius: 4px;
    }

    .card-header {
        background-color: #6cc112; /* Verde */
        color: white;
    }

    .btn-secondary {
        background-color: #DC143C; /* Rojo carmesí */
        border-color: #c11236;
        color: white;
    }

    .btn-secondary:hover {
        background-color: #c11236;
        border-color: #a60f2e;
    }

    .table th {
        background-color: #f8f9fa;
        color: #228B22;
    }

    .table-bordered thead th {
        border-bottom: 2px solid #228B22;
    }

    .text-right {
        text-align: right;
    }

    .card {
        margin-bottom: 20px;
        border: 1px solid rgba(0,0,0,.125);
        border-radius: 0.25rem;
    }
</style>
<h1>Agregar Nuevo Cliente</h1>

<?= Html::link('clientes/', 'Volver a la lista', 'class="btn btn-primary"') ?>

<div class="row">
    <div class="col-md-6">
        <?= Form::open('clientes/registrar', 'post', 'class="form-horizontal"') ?>

        <?= Form::hidden('clientes.id') ?>

        <div class="form-group">
            <?= Form::label('Nombre', 'Nombre', 'class="control-label"') ?>
            <?= Form::text('clientes.nombre', 'class="form-control" required') ?>
        </div>

        <div class="form-group">
            <?= Form::label('Razon Social', 'Razón Social', 'class="control-label"') ?>
            <?= Form::text('clientes.razon_social', 'class="form-control" required') ?>
        </div>

        <div class="form-group">
            <?= Form::label('E-mail', 'Email', 'class="control-label"') ?>
            <?= Form::email('clientes.email', 'class="form-control" required') ?>
        </div>

        <div class="form-group">
            <?= Form::label('Contraseña', 'Contraseña', 'class="control-label"') ?>
            <?= Form::password('clientes.password', 'class="form-control" required') ?>
        </div>

        <div class="form-group">
            <?= Form::label('Teléfono', 'Teléfono', 'class="control-label"') ?>
            <?= Form::text('clientes.telefono', 'class="form-control" required') ?>
        </div>

        <div class="form-group">
            <?= Form::label('Crédito', 'Crédito', 'class="control-label"') ?>
            <?= Form::number('clientes.credito', 'class="form-control" step="0.01" value="0"') ?>
        </div>

        <div class="form-group">
            <?= Form::label('Adeudo', 'Adeudo', 'class="control-label"') ?>
            <?= Form::number('clientes.adeudo', 'class="form-control" step="0.01" value="0"') ?>
        </div>

        <!-- Checkbox corregido manualmente -->
        <div class="form-group">
            <div class="checkbox">
                <label>
                    <input type="checkbox" name="clientes[activo]" value="1" <?= isset($clientes->activo) && $clientes->activo ? 'checked' : '' ?>> Activo
                </label>
            </div>
        </div>

        <div class="form-group">
            <?= Form::submit('Guardar Cliente', 'class="btn btn-success"') ?>
        </div>

        <?= Form::close() ?>
    </div>
</div>
