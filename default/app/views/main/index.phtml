<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Adelita's Pizza - Inicio</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #fff8f0;
            font-family: 'Arial', sans-serif;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        .header h1 {
            color: #d32f2f;
            font-size: 2.5rem;
            margin-bottom: 10px;
        }
        .header p {
            color: #555;
            font-size: 1.1rem;
        }
        .carousel-container {
            max-width: 900px;
            margin: 0 auto;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }
        .carousel-item {
            text-align: center; /* Centra la imagen horizontalmente */
        }
        .carousel-item img {
            max-width: 100%; /* Asegura que la imagen no exceda el ancho del contenedor */
            height: auto; /* Mantiene la proporción original */
            border-radius: 15px;
            display: inline-block; /* Para que el centrado funcione correctamente */
        }
        .carousel-inner {
            border-radius: 15px;
        }
        .carousel-indicators button {
            background-color: #328700;
        }
        .carousel-control-prev-icon,
        .carousel-control-next-icon {
            background-color: #328700;
            border-radius: 50%;
            padding: 15px;
        }
    </style>
</head>
<body>

<div class="header">
    <h1>Bienvenid@ a Adelita's Pizza</h1>
    <h2>Calidad y Sabor</h2>
</div>

<div id="pizzaCarousel" class="carousel slide carousel-container" data-bs-ride="carousel">
    <!-- Indicadores -->
    <div class="carousel-indicators">
        <?php for ($i = 0; $i < 17; $i++): ?>
            <button type="button" data-bs-target="#pizzaCarousel" data-bs-slide-to="<?= $i ?>" <?= $i === 0 ? 'class="active"' : '' ?>></button>
        <?php endfor; ?>
    </div>

    <div class="carousel-inner">
        <?php for ($i = 1; $i <= 17; $i++): ?>
            <div class="carousel-item <?= $i === 1 ? 'active' : '' ?>">
                <img src="/img/<?= $i ?>.jpg" alt="Pizza <?= $i ?>" class="d-block">
            </div>
        <?php endfor; ?>
    </div>

    <button class="carousel-control-prev" type="button" data-bs-target="#pizzaCarousel" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Anterior</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#pizzaCarousel" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Siguiente</span>
    </button>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>