<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos - Adelita's Pizza</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.4/css/dataTables.bootstrap5.min.css">
    <style>
        .content-header {
            background-color: #228B22; /* Verde forestal */
            color: white;
            padding: 10px 15px;
            border-radius: 4px;
        }

        .card-header {
            background-color: #6cc112; /* Verde */
            color: white;
        }

        .btn-secondary {
            background-color: #DC143C; /* Rojo carmesí */
            border-color: #c11236;
            color: white;
        }

        .btn-secondary:hover {
            background-color: #c11236;
            border-color: #a60f2e;
        }

        .table th {
            background-color: #f8f9fa;
            color: #228B22;
        }

        .table-bordered thead th {
            border-bottom: 2px solid #228B22;
        }

        .text-right {
            text-align: right;
        }

        .card {
            margin-bottom: 20px;
            border: 1px solid rgba(0,0,0,.125);
            border-radius: 0.25rem;
        }
    </style>
</head>
<body>

<a href="<?= URL_APP ?>/main" class="btn-regresar">Regresar</a>

<h1>Productos</h1>

<table border="1" class="table table-striped table-hover" id="myTable">
    <thead>
    <tr>
        <th>ID</th>
        <th>Nombre</th>
        <th>Precio</th>
        <th>Existencia</th>
        <th>Imagen</th>
    </tr>
    </thead>
    <tbody>
    <?php foreach ($productos as $p): ?>
        <tr>
            <td><?= $p->id ?></td>
            <td>
                <a href="<?= URL_APP ?>/productos/show/<?= $p->id ?>"><?= $p->nombre ?></a>
                <?= Html::link("productos/show/{$p->id}", "Ir a producto", ["class"=>"btn btn-sm", "title"=>"Ir a producto:"]) ?>
            </td>
            <td><?= $p->precio ?></td>
            <td><?= $p->stock ?></td>
            <td>
                <img src="<?= URL_APP ?>/IMG/<?= $p->imagen_url ?>" alt="<?= $p->nombre ?>" width="50">
            </td>
        </tr>
    <?php endforeach; ?>
    </tbody>
</table>

<!-- JS para tablas -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdn.datatables.net/1.13.4/js/jquery.dataTables.min.js"></script>
<script src="https://cdn.datatables.net/1.13.4/js/dataTables.bootstrap5.min.js"></script>
<script>
    let table = new DataTable('#myTable', {
        language: {
            url: "//cdn.datatables.net/plug-ins/1.13.4/i18n/es-MX.json"
        },
        responsive: true,
        pageLength: 5,
        lengthChange: false
    });
</script>

<footer>© 2025 Adelita's Pizza - Todos los derechos reservados.</footer>

</body>
</html>
